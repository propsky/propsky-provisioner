# ESP32 一鍵出廠設定工具 — 開發規格 v1.2

> 產品：SmartPay 投幣板（ESP32-WROOM-32E / MicroPython）｜2026-09-10 定稿

## 1. 目標

取代原本 Thonny 手動 9 步驟。燒錄人員每片只做：**按開始 → 等約 1 分鐘 → 照螢幕把小卡編號寫在板子上 → 看 LCD 正常後按「完成」**。

## 2. 環境

| 項目 | 內容 |
|---|---|
| 晶片 | ESP32-WROOM-32E |
| 韌體 | MicroPython v1.29.0（2026-08-24），`Generic ESP32 module with ESP32` |
| 燒錄器 | USB-TTL，DTR→EN、RTS→IO0（esptool 可自動進 bootloader） |
| 電腦 | Windows，同時接 1～4 個燒錄器 |
| 技術 | Python + pyserial（自寫 raw REPL）+ esptool + openpyxl，打包 .exe |

## 3. 目錄結構

```
出廠設定工具/
├─ provision.exe
├─ config.ini
├─ firmware/     ← 放「一個」.bin，例 ESP32_GENERIC-20260824-v1.29.0.bin
├─ payload/      ← 要上傳的 py 檔（以此資料夾內容為準）
├─ wifi.dat      ← 可選，格式 SSID;密碼（無換行）
├─ 出廠帳本.xlsx
├─ provision_log.csv   ← 每片即時流水帳
└─ logs/               ← 每片完整 serial log
```

## 4. 開工設定（每天一次）

1. 輸入**今天第一個小卡編號**（例 `F361`），之後每片自動 +1，保留前綴與位數。
2. 輸入 WiFi SSID／密碼，或載入 wifi.dat。SSID 不可含 `;`。
3. 啟動檢查：
   - firmware/ 內剛好一個 .bin，從檔名解析版本與日期
   - payload/ 不可為空；若內含 boot.py、token.dat、wifi.dat 則略過並提示
   - 備份帳本；檢查帳本內 MAC、token、小卡編號有無重複

## 5. 單片流程

| # | 步驟 | 規則 |
|---|---|---|
| 1 | 連線 | 開 port 前先設 DTR/RTS 為不作用，避免板子卡在 reset。送 Ctrl-C 中斷 main.py，進 raw REPL；失敗則用 DTR reset 後重試，最多 3 次 |
| 2 | 韌體檢查 | 讀 `os.uname()`，版本或日期與 .bin 不符、或 REPL 無回應 → esptool `erase-flash` + `write-flash 0x1000`，完成後回到步驟 1 |
| 3 | 讀 MAC | `machine.unique_id()` 轉大寫 hex（例 `9454C5536768`） |
| 4 | 決定身分 | MAC 已在帳本 → **沿用原小卡編號與 token**（重燒，不配新號）；否則為新板，進入步驟 7 才配號 |
| 5 | 清空＋上傳 | 刪除板上所有檔案，**保留 boot.py**；上傳 payload/*、wifi.dat、token.dat |
| 6 | 驗證 | 每個檔案在板上算 sha256 與本機比對 |
| 7 | 配號＋寫帳本 | 新板在此配下一個小卡編號；先寫 CSV 流水帳，再同步 xlsx |
| 8 | 重開機驗收 | `machine.reset()` 後讀 log，逾時 60 秒，見第 6 節 |
| 9 | 人工確認 | 大字顯示小卡編號與 MAC；人員寫編號、看 LCD 正常後按「完成」，該格回到待機，此時才拔板換下一片（先按再換）。LCD 不正常直接換一塊，按「重開機」只重跑步驟 8（不重傳檔案），不記錄 |

**token.dat**：程式產生 uuid4，小寫帶連字號，36 字元，**不換行**。
**配號時機**：步驟 7 才配。步驟 7 前失敗的板子不佔號；多 port 同時跑時，編號依到達步驟 7 的先後決定。

## 6. 驗收條件

**PASS（全部成立）：**

- log `Get token: <uuid>` 等於分配的 token
- log `My MAC Address: <mac>` 等於步驟 3 讀到的 MAC
- log 出現 `ESP Wi-Fi OK`
- 過程中沒有 `Traceback`
- 人員按「完成」（代表編號已寫、LCD 正常）

**只記錄、不影響判定：**

| 項目 | 來源 |
|---|---|
| 程式版本 | `版本為: SP3_V0.31a` |
| 訊號強度 | `WiFi Signal Strength: -50 dBm`（低於 -75 dBm 顯示警告） |
| MQTT | 人工確認期間背景繼續讀 log，出現 `MainStatus: STANDBY_MQTT` 記「OK」，否則記「未確認」 |

## 7. 帳本欄位

保留原三欄，後面追加：

| 小卡編號 | MAC Address (CPUID) | UUID (token) | 燒錄時間 | Port | MicroPython | 程式版本 | SSID | RSSI | MQTT | 結果 | 備註 |
|---|---|---|---|---|---|---|---|---|---|---|---|

- 不記 WiFi 密碼（CSV、xlsx、log 都不記）
- xlsx 被 Excel 開著時：先寫 CSV，xlsx 之後補寫，並提示關閉 Excel
- 重燒的板子更新同一列，不新增列

## 8. 錯誤處理

| 狀況 | 處理 |
|---|---|
| COM port 被佔用 | 提示關閉 Thonny 或其他序列埠程式 |
| 進不了 REPL | 自動重燒韌體；仍失敗則 FAIL |
| sha256 不符 | 重傳一次；仍不符則 FAIL |
| token 或 MAC 比對不符 | FAIL，顯示 log 裡的實際值 |
| 60 秒內沒有 `ESP Wi-Fi OK` | FAIL，提示檢查 WiFi 設定 |
| 出現 `Traceback` | 立即 FAIL，顯示錯誤行 |

**重跑同一片板子一定拿到同一組編號與 token**：步驟 7 前失敗 → 帳本沒寫，會重新配號；步驟 7 後失敗 → MAC 已綁定，會沿用。

## 9. 技術選型

| 項目 | 選擇 |
|---|---|
| 語言 | Python 3.12（64-bit，python.org 版） |
| 序列埠／raw REPL | pyserial（自寫，不用 mpremote，以掌控 DTR/RTS） |
| 燒韌體 | esptool 5.x（指令用 `erase-flash`、`write-flash`）；每個 port 用獨立子程序 |
| 帳本 | openpyxl |
| GUI（v1） | PySide6 |
| 打包（v1） | PyInstaller，需加 `--collect-data esptool` |
| 驅動 | USB-TTL 對應的 CH340 或 CP210x |

版本號於 POC 時固定在 requirements.txt。

## 10. 檔案上傳方式

採 raw REPL，與 Thonny 相同：

1. Ctrl-C 中斷程式 → Ctrl-A 進 raw REPL
2. 清空：送 `os.listdir()` + `os.remove()`，跳過 boot.py
3. 每個檔案：`f=open(name,'wb')` → 分段 `f.write(a2b_base64(...))`（約 1KB／段）→ `f.close()`
4. 在板上用 hashlib 算 sha256，與電腦端比對
5. 優先用 raw-paste 模式（有流量控制、較快），板子不支援時退回一般模式

預估速度約 8KB/s。若日後嫌慢，再評估改成預先產生 LittleFS 映像檔、用 esptool 一次燒入。

## 11. 開發階段

| 階段 | 範圍 |
|---|---|
| POC | 命令列、單一 port、完整流程 1～9 |
| v1 介面 | 見「出廠設定工具_介面草圖.html」：1024×768；分頁為燒錄／今日紀錄／Log 1～4；每格可選 COM port（待機時才能改、不可重複、可設未使用、⟳ 重新掃描、識別閃 TX 燈），設定存 config.ini |
| v1 | 圖形介面，4 格，每格一個開始鈕、目前步驟、大字 PASS／FAIL 與小卡編號；一個程式統一配號 |
| 之後 | 帳本改 Google Sheets（多台電腦共用）、直接上傳雲端 |
