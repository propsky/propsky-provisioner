# Code Review 第六輪（2026-09-11）

已讀 `2026-09-11_round5_report.md`，對照程式碼驗證。`python -m pytest -q`：6 passed，版本 0.1.4 兩處同步。

## Round 5 修正複審結果

| Round 5 項目 | 驗證結果 |
|---|---|
| P2 新板開始時殘留上一片狀態 | ✓ 已修好（`start_slot` 建立全新 `SlotSnapshot` 並替換 `self.snapshots[index]`，`ui.py:417-424`；`_on_log`/`_on_step`/`complete_slot` 引用的都是同一個新物件，無懸空引用；`retry_slot` 正確保留原身分資料） |
| 終端深色樣式失效 | ✓ 已修好（selector 改為 `QPlainTextEdit#terminal`，`ui.py:612`） |

## 新發現

無。本輪掃描沒有發現新的 P0/P1/P2。

## 結論

**六輪下來所有 review 事項全部結案，目前沒有任何未結案的問題。** 程式碼側能驗的都驗過了：協定層有 fake serial 測試、帳本一致性有測試、UI 渲染有 offscreen 截圖驗證。

下一步建議轉入**實機驗證**，這些只有真板子能證明：

1. DTR/RTS 時序——確認開 port 不會讓板子重開或進 bootloader（接線是否有電晶體電路會影響行為）。
2. raw REPL 實際流量——大檔上傳（無 raw-paste 流控）在 115200 下是否穩定、逾時參數是否夠。
3. 完整流程 1～9 跑一片新板 + 重燒同一片，確認配號沿用與帳本更新。
4. 打包 PyInstaller 後確認 esptool.exe 找得到、`--collect-data esptool` 有帶。

已知延後項目（不擋上線）：raw-paste 模式、MQTT 僅在 60 秒開機視窗內判定。
